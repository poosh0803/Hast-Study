// Registry of Math content sets. Add a new set:
//   1. Create content/math/sets-<id>.js exporting a set object.
//   2. Import it below and add it to MATH_SETS.
// Nothing outside this file (and the new set file) needs to change —
// app/math/page.js just reads MATH_SETS.

export const MATH_SETS = [];
