// Registry of Abstract content sets. Add a new set:
//   1. Create content/abstract/sets-<id>.js exporting a set object.
//   2. Import it below and add it to ABSTRACT_SETS.
// Nothing outside this file (and the new set file) needs to change —
// app/abstract/page.js just reads ABSTRACT_SETS.

export const ABSTRACT_SETS = [];
