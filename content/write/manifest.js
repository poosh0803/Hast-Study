// Registry of Write content prompts. Add a new prompt:
//   1. Create content/write/prompts-<id>.js exporting a prompt object.
//   2. Import it below and add it to WRITE_PROMPTS.
// Nothing outside this file (and the new prompt file) needs to change —
// app/write/page.js just reads WRITE_PROMPTS.

export const WRITE_PROMPTS = [];
